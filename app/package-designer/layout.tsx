import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Package Designer | Aprimo Editor Tools",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
